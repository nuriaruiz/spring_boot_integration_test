# Scripts SQL controlled by @Sql annotation in tests
spring.sql.init.mode=never
# Optional: show SQL in logs (handy when debugging)
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
